import styled from 'styled-components/native';
import {
  alignSelf,
  alignItems,
  alignContent,
  width,
  height,
  flex,
  borderColor,
  left,
  right,
  top,
  bottom,
  backgroundColor,
  space,
  borderBottom,
  borderRadius,
  borderLeft,
  borderRight,
  borderWidth,
  borderTop,
} from 'styled-system';

const Box = styled.View`
  ${alignSelf}
  ${alignItems}
  ${alignContent}
  ${width}
  ${height}
  ${flex}
  ${borderColor}
  ${left}
  ${right}
  ${top}
  ${bottom}
  ${backgroundColor}
  ${space}
  ${borderBottom}
  ${borderRadius}
  ${borderLeft}
  ${borderRight}
  ${borderWidth}
  ${borderTop}
`;

export default Box;
